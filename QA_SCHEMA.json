{
  "user_version": 18,
  "tables": 49,
  "fts_shadow_tables": 6,
  "indexes": 35,
  "triggers": 55,
  "integrity": "ok",
  "foreign_key_check": [],
  "locked_damage_mutation_blocked": true,
  "cross_matter_damage_reference_blocked": true,
  "approved_legal_sections_mutation_blocked": true,
  "approved_legal_paragraphs_mutation_blocked": true,
  "approved_ruleset_mutation_blocked": true,
  "engine_run_snapshot_immutable": true,
  "superseded_ruleset_mutation_blocked": true,
  "superseded_to_approved_reversal_blocked": true,
  "engine_run_status_reversal_blocked": true,
  "matter_profile_and_parties_cascade_delete_with_matter": true,
  "matter_workstreams_cascade_delete_with_matter": true,
  "matter_requirements_cascade_delete_with_matter": true,
  "verified_ledger_entry_fields_immutable_but_stale_carve_out_works": true,
  "verified_ledger_entry_stale_flag_is_one_directional_0_to_1_only": true,
  "verified_ledger_entry_sources_immutable": true,
  "verified_ledger_entry_delete_blocked_unless_cascade_guard_active": true,
  "matter_ledgers_cascade_delete_with_matter_even_when_verified_via_guard": true,
  "cross_matter_ledger_supersession_blocked": true,
  "only_one_verified_successor_per_superseded_ledger_entry": true,
  "verify_rejects_a_source_whose_document_version_has_gone_stale": true,
  "document_pages_fts_stays_in_sync_via_triggers_for_insert_update_and_cascade_delete": true,
  "retrieval_backfill_of_pre_existing_pages_is_idempotent": true,
  "regenerated_at": "2026-08-26",
  "regenerated_against_parent_commit": "b7884df676f1c034f206e0d0863050888180d9ff",
  "regenerated_by": "python3 sqlite3 against src-tauri/migrations/001_schema_v12.sql + 002_legal_rules_infrastructure_v13.sql + 003_matter_profile_v14.sql + 004_matter_workstreams_v15.sql + 005_matter_requirements_v16.sql + 006_matter_ledgers_v17.sql + 007_retrieval_context_v18.sql, all applied in order (real, not estimated), 3 times in a row to confirm idempotency. Phase B milestone B5a (Focused AI Retrieval) adds no new application table - `tables` stays 49 - only a local FTS5 index (`document_pages_fts`, an ordinary virtual table plus 5 SQLite-managed shadow tables: _data/_idx/_docsize/_config/_content, counted separately as `fts_shadow_tables` so this field can never silently absorb them into the real-table count) kept in sync with `document_pages` via 3 new triggers (INSERT/UPDATE OF normalized_text/DELETE - the delete trigger alone correctly handles cascade deletes too, verified empirically: SQLite fires a child row's own triggers even for a cascaded delete) plus one idempotent, incremental backfill statement for rows that existed before the migration ran. No new index. `rusqlite` 0.37 has no `fts5` Cargo feature - FTS5 is already compiled into the bundled SQLCipher build this project uses; confirmed at runtime, not assumed, by a permanent `fts5_is_available_in_this_sqlite_build` test that runs on every `cargo test --locked` including real Windows CI. Free-text queries are never handed raw to `MATCH` - each term is phrase-quoted (embedded quotes escaped) and OR-joined before compilation, so FTS5 query-syntax operators (quotes/AND/OR/NOT/parens/NEAR/*/^) a lawyer might type can never be interpreted as query syntax. Ranking is an explicit deterministic sort tuple (bm25 ascending, category-boosted as a tie-break only, version recency, page, block, source_id) - never a blended arbitrary-scale score. `matter_id` and `document_versions.stale=0` are re-applied against live tables at every stage, including neighbor expansion, never trusted from the FTS index. An oversized single-row `anchor_kind='document'` source is never dropped for exceeding the budget - it is deterministically windowed (fixed size, centered on the first query-term match, same input always produces the same window/hash) while the manifest still carries the source's real, unchanged `sourceId`/`textSha256` for its full text. `ContextManifest` carries a canonical, non-circular `manifest_sha256` (hashed over a payload type with no hash field of its own) so identical inputs on the same DB always produce a byte-identical manifest across runs. 10 new integration tests (cross-matter isolation, stale exclusion even when it would rank first, relevance-beats-recency, neighbor expansion with `neighborOfSourceId`, oversized-document windowing + determinism, full-pipeline determinism, char-based budget enforcement, empty/no-match behavior, every manifest source resolves to a real live page, preview/run parity) plus 8 new unit tests in retrieval.rs (ranking tie-break rules, FTS query compilation incl. Hebrew/Arabic/quotes/parens, deterministic windowing) - 127/127 total. cargo check/test --locked, npm run build, contract:check (106/106, no drift - only new optional payload fields, no new commands), qa:static (now checking migration 007's virtual table/triggers/backfill and that retrieval.rs never MATCHes a raw query) all pass. Migration re-verified idempotent via direct sqlite3 (applied 3x): 49 real tables + 6 FTS shadow tables, 35 indexes, 55 triggers, user_version=18, integrity ok, fk_check clean; insert/update/delete-direct/delete-cascade-via-matter sync and post-migration backfill all verified against a live in-memory DB, not merely asserted."
}
